# Open 遮罩移除 · 真机点测清单（2026-08-09）

> 验收方式：QA（严过关）独立复验 **IS_PASS: YES**（构建可复现、grep 门禁全绿、tsc 0、源码通读无问题）。
> 以下需在 Chromium 手动执行，QA 无法替代。任一项不符请**截图回报**。

## 准备
1. Chrome/Chromium 加载 `dist-extension/` 未打包扩展（本清单对应构建 02:03，已重启加载）。
2. 用浏览器直接打开本地 `file:///…/xxx.md` → 出现 MDnote 编辑器。

## ① 直弹验证（本次核心）
- [ ] 点工具栏 **Open** → 应**立即弹出操作系统原生文件对话框**
- [ ] ❌ 不出现 "Open a Markdown file" 半透明中间遮罩
- [ ] ❌ 不需要"点两次"（旧：先点遮罩里的 Open 再弹选择器）

## ② 打开渲染验证
- [ ] 选一个 `.md` → 内容**渲染进当前编辑器面板**
- [ ] ❌ 当前页面不跳转/换页；❌ 不新开目录标签页；❌ 不弹 "file browser opened in a new tab"

## ③ 取消路径验证（安静回退）
- [ ] 点 Open 后原生对话框点**取消**或按 **Esc** → 安静回到原状（内容不变）
- [ ] ❌ 无报错弹窗、❌ 无死循环、❌ 不再弹任何兜底遮罩

## ④ 兜底层不应出现（正常路径）
- [ ] 正常点 Open 流程中**不应**看到 "Click to open" 兜底遮罩
- [ ] ⚠️ 若它出现 → 说明极稀有 user activation 未传播，**请截图+记录操作**回报

## ⑤ Open 后接 Save 零选择器
- [ ] Open 成功后改几字 → 点 **Save**
- [ ] 首次可能弹 Chrome 原生"允许编辑此文件"一次性授权（正常）→ 允许后直写原文件
- [ ] 之后再改再 Save → 静默直写，全程零文件/目录选择器

## ⑥ 回归不退化（Save 侧 R7 / RACE-01）
- [ ] **RACE-01**：连续快速多次 Save（或自动保存与手动 Save 交叠）→ 回执正确匹配、无错乱、无重复弹窗
- [ ] **冷启动 Save（R7）**：新开**从未走过 Open** 的 `file:///…/xxx.md`，直接改内容点 Save → 弹**一次**"另存为"（按当前目录智能定位、预填原文件名）→ 确认后直写；后续 Save 零选择器
- [ ] 冷启动下的自动保存应静默（不弹任何选择器）

## 已知风险（非本次引入，R7 遗留，需你判断严重程度）
`savedPathOf()` 只比对 basename，`startIn` 进不了子目录。打开 `~/Downloads/sub/deep/r7-deep.md` 时另存为弹窗停在 `~/Downloads` 根，**若不导航直接确认，改动会写进上层同名文件（甚至静默覆盖），而编辑器回显路径仍是深层的（回显不可信）**。R7 只承诺"按目录定位+预填"，FSAA 本身到不了任意子目录。建议真机确认是否需补"落盘后校验完整路径"。
