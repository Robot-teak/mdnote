# MDnote 更新日志

## v0.5.0 (桌面) / v0.3.0 (插件) (2026-09-22)

预览增强迭代：Mermaid 图表、编辑 ⇄ 预览双向精确定位，以及一批期待已久的预览便利能力。两条产品线共用同一批共享能力，插件版另有两项界面调整。

### 桌面版

**新功能**

- **Mermaid 图表预览**：info string 为 `mermaid` 的围栏代码块渲染为真实图表，共 11 种图类型——flowchart、sequenceDiagram、classDiagram、stateDiagram、erDiagram、gantt、pie、journey、timeline、gitGraph、mindmap；图表样式在预览、主题切换与导出文件中保持一致
- **单块 Diagram / Source 切换**：任意单个图表可切回源码显示，不影响文档中的其他图表
- **图表放大与 SVG 导出**：点击图表在更大的浮层中查看，浮层提供 **Download SVG**（本地保存，不经服务器）与 **跳到源码**（关闭浮层并定位到该图表的源码行）；`Esc`、✕、点击浮层外部均可关闭
- **预览点击跳转源码**：分屏模式下点击预览任意元素，编辑器光标移到产生该元素的源码行、滚动到可视区并闪烁高亮；独占预览模式会记住这次跳转，切回分屏后再执行
- **预览行号（可选）**：预览左侧槽显示块级行号，编号与编辑器一致；默认关闭，在 **设置 → 预览 → 预览行号** 开启；跟随预览字号与明暗主题，且不会写入导出的 HTML
- **代码块复制按钮**：预览区每个代码块右上角都有复制按钮（含切到 Source 态的 Mermaid 块），复制成功提示「✓ Copied」
- **锚点链接在预览内生效**：点击 `#heading` 链接滚动到对应标题，且不改变页面 URL

**Bug 修复**

- **宽表格不再撑宽预览区**：列数多的表格改为在自身容器内横向滚动（带主题化滚动条），不再把预览区顶宽；打印时也按完整宽度输出，不再被裁切

**技术改进**

- **编辑 → 预览定位精度提升**：≤256KB 的文档改用行级锚点，从编辑器跳转落在正确的行而不是就近的块；更大的文档仍沿用块级定位。自动切换，无需配置
- **同步闪烁 300ms → 600ms**：两侧高亮均保持 600ms
- **Mermaid 图表配色改为低饱和冷色系（明暗两套）**，相邻色相差异经 ΔE00 ≥ 10 校验，避免多色图难以区分；（配色归档见 `deliverables/preview-enhance-v0.5.0/91-mermaid-palette-archive-2026-09-21.md`）
- **Mermaid 可关闭**：**设置 → 预览 → Mermaid 图表**（默认开启）；关闭后 Mermaid 块按普通代码块渲染，且不加载 Mermaid 引擎
- **不用就不付代价**：Mermaid 引擎懒加载，仅当文档中确实存在 Mermaid 块时才加载；没有图表的文档保持原有启动速度与内存占用
- **未随包发布的图类型**：C4、architecture、Venn、XY chart、Sankey、Cynefin，以及图表标签内的 LaTeX 公式未包含；使用这些类型的块会回退为源码并给出简短提示，不会破坏预览

### Chrome 插件版（MV3）

**新功能**

- **Mermaid 图表预览**：info string 为 `mermaid` 的围栏代码块渲染为真实图表，共 11 种图类型——flowchart、sequenceDiagram、classDiagram、stateDiagram、erDiagram、gantt、pie、journey、timeline、gitGraph、mindmap；图表样式在预览、主题切换与导出文件中保持一致
- **单块 Diagram / Source 切换**：任意单个图表可切回源码显示，不影响文档中的其他图表
- **图表放大与 SVG 导出**：点击图表在更大的浮层中查看，浮层提供 **Download SVG**（经浏览器保存，不经服务器）与 **跳到源码**（关闭浮层并定位到该图表的源码行）；`Esc`、✕、点击浮层外部均可关闭
- **预览点击跳转源码**：分屏模式下点击预览任意元素，编辑器光标移到产生该元素的源码行、滚动到可视区并闪烁高亮；独占预览模式会记住这次跳转，切回分屏后再执行
- **预览行号（可选）**：预览左侧槽显示块级行号，编号与编辑器一致；默认关闭，在 **设置 → 预览 → 预览行号** 开启；跟随预览字号与明暗主题，且不会写入导出的 HTML
- **代码块复制按钮**：预览区每个代码块右上角都有复制按钮（含切到 Source 态的 Mermaid 块），复制成功提示「✓ Copied」
- **锚点链接在预览内生效**：点击 `#heading` 链接滚动到对应标题，且不改变页面 URL
- **首页改为列出全部未保存草稿**：不再只显示最近一条，而是以表格列出每一份未保存草稿，含 **名称 / 最后更新 / 操作** 三列；每行各有 **恢复** 与 **丢弃** 按钮，按更新时间倒序，条目多时可滚动

**功能移除**

- **最近文件面板**：首页的「最近打开的文件」面板已移除，改用 **Open File** 重新打开文档

**Bug 修复**

- **宽表格不再撑宽预览区**：列数多的表格改为在自身容器内横向滚动（带主题化滚动条），不再把预览区顶宽；打印时也按完整宽度输出，不再被裁切

**技术改进**

- **草稿不再过期**：取消原先的 24 小时过期规则，草稿会一直保留在列表中，直到你主动丢弃
- **启动时清理残留草稿记录**：启动时清掉仍指向文件的草稿记录（例如保存失败留下的）；磁盘上的文件不会被改动，仍可正常打开；仅存在于这些残留记录中的编辑内容不会被恢复
- **编辑 → 预览定位精度提升**：≤256KB 的文档改用行级锚点，从编辑器跳转落在正确的行而不是就近的块；更大的文档仍沿用块级定位。自动切换，无需配置
- **同步闪烁 300ms → 600ms**：两侧高亮均保持 600ms
- **Mermaid 图表配色改为低饱和冷色系（明暗两套）**，相邻色相差异经 ΔE00 ≥ 10 校验，避免多色图难以区分；（配色归档见 `deliverables/preview-enhance-v0.5.0/91-mermaid-palette-archive-2026-09-21.md`）
- **Mermaid 可关闭**：**设置 → 预览 → Mermaid 图表**（默认开启）；关闭后 Mermaid 块按普通代码块渲染，且不加载 Mermaid 引擎
- **不用就不付代价**：Mermaid 引擎懒加载，仅当文档中确实存在 Mermaid 块时才加载；没有图表的文档保持原有启动速度与内存占用
- **未随包发布的图类型**：C4、architecture、Venn、XY chart、Sankey、Cynefin，以及图表标签内的 LaTeX 公式未包含；使用这些类型的块会回退为源码并给出简短提示，不会破坏预览

---

## v0.4.2 (桌面) / v0.2.1 (插件) (2026-08-10)

本版主体是插件线 v0.2.x 的预览稳定性与设置项完善；桌面版以「共享层修复 + 版本号发布」的形式带上同一批改动。

### 桌面版

**Bug 修复**

- **更新检查改回直接请求**：去掉更新检查的中间代理层，改回直接 fetch GitHub releases API
- **更新检查失败提示更具体**：失败时带上 GitHub 返回的错误正文，不再只显示笼统的失败
- **预览默认字号 16 → 14**：与编辑器默认字号对齐，新装用户不再看到预览明显大于源码（共享代码，桌面版与插件版同时生效；已手动选过预览字号的用户保留原值）

**技术改进**

- **版本号统一升级至 0.4.2**：`package.json` / `src-tauri/tauri.conf.json` / `src-tauri/Cargo.toml`（含 `Cargo.lock`）/ `src-tauri/build_dmg.py`（决定 DMG 文件名与 `CFBundleShortVersionString`）/ `AboutDialog` 桌面分支，共五处同步；插件分支仍为 0.2.1，两条版本线保持独立
- 桌面版本轮无独立功能改动，随本版发布的是共享层的预览修复：预览稳定渲染、预览字体族与字号独立、分屏左右互换、自动保存重做

### Chrome 插件版（MV3）

**新功能**

- **浏览器打开 `.md` 文件直接在文件页渲染**：在 `.md` 文件页内联注入编辑器，文档就地渲染，不再单独开标签页；工具栏 Open 按钮以 `SHOW_OPEN_BUTTON=false` 隐藏，代码与处理函数保留以便后续恢复
- **设置面板重做为四个标签页**：Editor / Preview / Behavior / Auto-Save，支持方向键导航，宽度放宽到 520px；新增预览字体族、预览字号、分屏布局、自动保存间隔四项控件，并在内联模式给出间隔下限提示
- **分屏左右互换**：新增 `SplitLayout` 设置与 `.split-reversed`（`row-reverse`），可翻转编辑器 / 预览的左右顺序并同步分隔线边框
- **自动保存间隔可配置**：10s / 30s / 1m / 2m / 3m / 5m / 从不，以 `settings.autoSaveInterval` 为唯一真源（0 = 从不）；状态栏显示生效间隔并与设置面板双向联动，内联 iframe 模式保留 15s 下限以避开消息桥超时

**Bug 修复**

- **选完文件「什么都没发生」**：修复「当前页非空 → 打开 `file://` 目录列表」的死分支——该分支在 3 个 `await` 之后用户手势已过期，被弹窗拦截后静默 no-op。改为按当前页是否为空分流：空文档就地加载；有内容则紧跟文件选择器同步 `window.open`，用当前目录 + 文件名拼出 `file://` 文档页开新页签，当前页未保存内容保留。另加 `openNewTabWithFallback` 手势兜底，被拦截时收一次真实点击补回激活再开，彻底消灭静默失败
- **预览文本不跟随字体设置**：`.preview-pane pre` / `.preview-pane table` 的硬编码 13.5px 改为 `0.95em`，并引入 `--preview-font-size` / `--preview-font-family` CSS 变量由 `applySettingsToCSS` 写入；预览默认改用比例字体栈，不再继承编辑器的等宽字体
- **打字时预览闪烁**：`PreviewPane` 改为保持恒定 DOM 容器，用 `useLayoutEffect` + last-written-HTML ref 写入 `innerHTML` 并保留 `scrollTop`（仅在切换文件时回到顶部）；加载态与空态改为绝对定位浮层，不再每次防抖都重建整棵子树；组件订阅改用 selector，增量更新不再置 `isPreviewLoading`，并去掉每次按键的 `setSavedScrollTop` 全量写入
- **预览默认字号 16 → 14**：与编辑器默认字号对齐
- **更新检查链路简化**：移除经由 background service worker 代理的链路，改回直接 fetch；失败提示带上 GitHub 返回的错误正文
- 背景页回执链路、消息桥接、自动保存与样式等一批插件版修复随 v0.2.0 一并落地

**技术改进**

- **插件版本线 0.1.9 → 0.2.0**（`manifest.json` + `AboutDialog`），随后随 v0.2.1 发布
- **Onboarding 文案修正**：Open 按钮 → Open File 按钮，`Cmd+O` → `Cmd+Shift+O`
- **manifest 清理**：移除重复的 `host_permissions` 块

### 发布后修复（v0.4.2 / v0.2.1 tag 之后，随下一个构建发布）

- **更新检查更稳健**：GitHub releases API `per_page` 5 → 30，避免最新桌面版被一串插件版挤出抓取窗口；改为在匹配的 tag 中取 semver 最大值，不再依赖 API 的发布时间顺序（重新发布或插件版交错发布时可能把旧版本当成最新）。v0.4.1 用户需手动升级到 v0.4.2 一次
- **README 同步**：版本表插件线 `0.1.x` → `0.2.x`（示例 `extension-v0.2.1`）、桌面示例 `desktop-v0.4.1` → `desktop-v0.4.2`；桌面 Auto-Save 说明改为可配置间隔（10s–5m / off）；安装说明的「已损坏」处置统一为 `xattr -cr`

---

## v0.4.1 (2026-07-31)

### 桌面版

**新功能**
- 设置面板（SettingsDialog）：编辑器样式 / 预览区样式 / 编辑器行为三个分区，所有设置持久化到 localStorage，支持一键恢复默认（工具栏齿轮图标入口）
- 版本号统一升级至 0.4.1（package.json / tauri.conf.json / Cargo.toml / build_dmg.py / AboutDialog）

**Bug 修复**
- 修复视图切换快捷键 `Cmd+Option+1/2/3`（编辑器 / 分屏 / 预览）在 macOS 下失效的问题（改用物理键位 `e.code` 判定，兼容 `Cmd`/`Ctrl` + `Option`）
- 修复构建脚本 `build.rs` 缺失导致前端资源无法正确嵌入、全新克隆构建失败与白屏的问题

**技术改进**
- 恢复标准 Tauri 2 `build.rs`（`tauri_build::build()`），在编译期将 `dist` 嵌入二进制，确保发布的桌面程序始终包含最新前端

### Chrome 插件版（MV3）

**插件能力**
- 工具栏图标 / `Ctrl+Shift+M`（Mac `Command+Shift+M`）快捷键打开编辑器标签页
- File System Access API：打开/保存本地 Markdown 文件，支持目录句柄与图片文件选择
- 本地草稿：未保存内容 60 秒自动写入 IndexedDB，三态保存指示（编辑中 / 草稿已存 / 磁盘已存）
- 最近文件：左侧栏「目录」区展示，支持点击恢复草稿、单条删除、一键清空
- 多标签页文件锁：基于 `chrome.storage.session` 的并发写保护，句柄失效时提示重新授权
- 首次使用引导（Onboarding）、XSS 防护（DOMPurify 过滤）、导出 HTML/PDF、代码块主题
- 构建产物 `dist-extension/` 可直接在 `chrome://extensions` 以「加载已解压的扩展程序」方式安装

**技术要点**
- MV3 manifest：仅申请 `storage` / `downloads` / `contextMenus` 权限（无 tabs），最低 Chrome 102
- 构建：`npm run build:ext` + `npm run verify:ext`（11 项产物校验），CI 待接入

### 测试反馈修复（v0.4.1 第二轮，产物版本 0.4.2）

- **New/Open 不再替换当前窗口**：插件版当前窗口已加载文档时，New/Open 自动在**新标签页**打开，当前内容不受影响（原行为会直接替换导致未保存内容丢失）
- **文件锁误报修复**：标签页关闭时立即清理其持有的文件锁（监听 `tabs.onRemoved`），打开文件前校验锁主标签页是否存活——不再出现"窗口已关闭仍提示只读"（原 30 分钟超时过慢）
- **草稿恢复提示条**：欢迎页检测到无句柄的未保存草稿时提示「恢复 / 丢弃」，并明确"草稿仅保存在浏览器本地，尚未写入磁盘"；拖拽/恢复打开的文件也会记入最近文件
- **浏览器直接打开 .md 文件接管**：新增 content script（`content-md.js`），在 `file://` 或网页的 Markdown 文件页注入「用 MDnote 打开」按钮，一键在编辑器中打开（`file://` 需在扩展详情勾选「允许访问文件网址」）

### 第三轮体验升级（产物版本 0.4.3）

- **浏览器打开 .md 文件自动渲染**（MarkView 模式）：页面自动变成 Markdown 渲染样式（标题/代码高亮/表格/明暗自适应），顶部显示「由 MDnote 渲染」，右上角保留「MDnote ✎ 编辑」按钮
- **自动保存写回磁盘原文件**：打开过的文件（有句柄且权限有效时）每 60 秒自动写回原文件，无需手动保存；权限过期或新建文档时降级为草稿并提示恢复
- content script 改为 esbuild 打包 IIFE（`scripts/build-content.mjs`），内联 markdown-it + highlight.js，规避 content script 动态 import 扩展模块的兼容性问题

### 第四轮修复（产物版本 0.4.4）

- **本地 .md 文件（file://）自动渲染修复**：content script 读取内容增加兜底——`fetch(file://)` 受 CORS 限制失败时，直接读取页面已渲染的纯文本（本地文件以 text/plain 显示），按钮打开不再报"打开失败"
- **新建文档自动保存草稿修复**：自动保存定时器改为常驻（不再因新建文档内容为空被清除），新建文档输入内容后自动存草稿；「保存」选择路径后自动清理旧草稿
- 应用内 New/Open 新标签页、自动保存写回原文件行为保持

### 第五轮重构（产物版本 0.4.5，按测试反馈重做）

- **去掉渲染步骤，打开 .md 直接跳转编辑器**：浏览器打开本地/网页 .md 文件时，content script 自动读取内容并打开 MDnote 编辑器注入（相当于自动点击编辑按钮），不再渲染中间页面
- **未修改不保存**：自动保存增加 `isDirty` 检查——打开文件什么都没做时，不触发任何保存动作，切换视图模式也不会变成 unsaved
- **保存规则重做**：
  · 本地打开的文件（有路径）→ 仅在有修改时自动写回原文件，不存草稿
  · 新建文档（无路径）→ 自动存临时草稿
  · 草稿废弃（新建后放弃）或保存到其他路径（另存为）→ 自动清理旧草稿
- **左下角显示完整文件路径**（与桌面版一致），浏览器打开的 file:// 文件解析出完整路径显示
- content-md.js 精简为纯自动跳转（858B，移除 markdown-it/hljs 渲染依赖）

### 第六轮修复（产物版本 0.4.6，保存相关）

- **切换视图模式不再误置"未保存"**：dirty 状态只由用户编辑触发（EditorPane updateListener），移除 App.handleContentChange 里的无条件 setDirty（打开文件/挂载编辑器不再污染状态）
- **保存反馈修正**：saveAs 返回保存结果——用户取消保存时不再提示"Saved!"；⌘S 快捷键与工具栏保存统一走同一逻辑
- **最近文件恢复绑定磁盘句柄**：从最近文件恢复的文档保存时直接写回原文件（不弹另存为）
- 说明：浏览器直接打开的文件（file://，无写入权限）保存时需选择保存位置——浏览器安全限制，无法静默写回

### 第七轮：主动排查修复（产物版本 0.4.7）

- **编辑后刷新/关闭页面丢内容**（主动排查发现）：自动保存原本最长等 60s，刷新前可能没落盘。新增**内容变化后 3 秒防抖快速保存**（有写入权限写回原文件，新建文档存草稿），刷新/关闭后可从「恢复草稿」找回。已真机验证：输入 → 5s 内草稿落库 → 刷新 → 恢复条出现 → 恢复成功
- 全面冒烟审计：编辑/视图切换/撤销/设置对话框/导出入口无运行时错误；草稿恢复条、路径显示、自动跳转均正常
- README 安装说明固化（output/README-INSTALL.txt，打包直接引用）

### 第八轮（产物版本 0.1.8，插件版独立版本线）

- **插件版版本号独立**：改为 0.1.8 起，不再沿用桌面版 0.4.x
- **About 改造**：主标题 MDnote、副标题 Chrome Extensions、版本 0.1.8；文案全部英文（插件版/桌面版分别显示）；移除未上架前无意义的更新检查
- **浏览器打开的文件修改后自动保存**：无句柄文件（浏览器打开/新建）修改后统一自动存草稿（原逻辑"有路径不存草稿"导致浏览器打开的文件修改后不保存）
- **目录授权直写（保存不再弹"另存为"）**：保存/自动保存时优先用已授权目录按文件名免弹窗写回原文件；未授权时首次弹一次"选择文件所在目录"，此后该目录文件保存/自动保存均直接写回。IndexedDB 升级 v2 新增 dirs store 存储目录句柄

### 第九轮调整（产物版本 0.1.8 修订）

- **恢复 About「Check for Updates」与 compareVersions**（插件后续发布到 GitHub 后用于版本检查）
- **About 弹窗放宽 30%**（360px → 468px），观感更舒适
- **浏览器打开的文件保存策略调整**（用户明确）：
  · 不存草稿（有路径）
  · 无修改 → 不请求任何权限
  · 有修改 → 自动/手动保存时弹一次「定位原文件」，拿句柄写回**原路径**并缓存，此后保存/自动保存直写原文件
  · 移除目录授权方案（不需要选目录）
- 新建文档（无路径）仍存临时草稿

### 已知局限（后续版本处理）
- 插件版本地图片在预览区暂以 `[Image: xxx]` 占位符显示，完整预览待后续接线
- 多标签页文件锁基于草稿 ID，同一文件跨标签页的并发检测待优化（需 `isSameEntry` 稳定文件 ID）

---

## v0.4.0 (2026-05-16)

### 新功能

**快捷输入增强**
- F1: `[` 自动补全链接格式 `[]()` — 输入 `[` 自动插入完整链接语法，Tab 键在 `[]` 和 `()` 之间跳转，`]` 智能跳转到 URL 输入位
- F2: `![` 图片路径补全 — 输入 `![` 自动触发图片路径提示，支持 Tauri 文件对话框选择本地图片，自动填充相对路径
- F3: 表格快速生成 — 输入表头行（如 `| A | B |`）后按 Enter，自动补充分隔线行和内容行

**精细化样式定制**
- F4: 编辑器字体 / 字号 / 行高自定义 — 设置面板中可选择字体、调整字号（12~24px）、行高（1.0~2.5）
- F5: 代码块高亮主题 — 6 套 highlight.js 主题可选：GitHub / GitHub Dark / Monokai / Atom One Dark / VS Code / VS 2015，支持跟随系统主题自动切换
- F6: 缩进量 + 预览段落间距 — 可选 2/4/8 空格缩进，段落间距可调（0.5em~2em）

**设置面板**
- 新增 SettingsDialog 设置面板，Toolbar 齿轮图标入口
- 三个分区：编辑器样式 / 预览区样式 / 编辑器行为
- 所有设置持久化到 localStorage，支持一键恢复默认

### Bug 修复
- 修复 F2 图片文件对话框选择后路径无法插入文档的问题（sliceString 长度 + parenPos 偏移错误）
- 修复代码块主题手动选择后会被系统主题切换覆盖的问题（codeBlockThemeManuallySet 状态管理）

### 技术改进
- 新增 `EditorSettings` 接口，统一管理所有编辑器设置项
- 使用 CM6 Compartment 实现字体/字号/行高/缩进/换行/行号的动态切换
- CSS 变量驱动编辑器和预览区样式，实现响应式更新
- hljs 主题通过动态创建 `<link>` 标签加载，避免样式冲突
- Tab 跳转 keymap 注册在 `indentWithTab` 之前，确保优先级正确

---

## v0.3.8

- 基础 Markdown 编辑与预览
- 明暗主题切换
- 文件打开/保存/导出 HTML/PDF
- 目录侧栏
- 查找替换
- macOS 文件关联打开
